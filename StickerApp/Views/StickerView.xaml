<Window
    x:Class="StickerApp.Views.StickerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sticker">

    <Grid x:Name="RootGrid" CornerRadius="12" BorderThickness="1" BorderBrush="#20000000">
        <Grid.Background>
            <SolidColorBrush x:Name="BackgroundBrush" Color="#FFF8B0" />
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header / Control Panel (Fades in on mouse hover) -->
        <Grid x:Name="HeaderPanel" Grid.Row="0" Height="36" Background="#15000000" Opacity="0.0" PointerPressed="Header_PointerPressed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock x:Name="TitleTextBlock" Text="Reminder" VerticalAlignment="Center" Margin="12,0,0,0" FontWeight="SemiBold" FontSize="12" Foreground="#80000000" />

            <!-- Window Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" Margin="0,0,8,0" VerticalAlignment="Center">
                <!-- Edit button -->
                <Button x:Name="EditButton" Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0" Click="EditButton_Click">
                    <FontIcon Glyph="&#xE70F;" FontSize="12" Foreground="#80000000" />
                </Button>
                <!-- Lock button -->
                <Button x:Name="LockButton" Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0" Click="LockButton_Click">
                    <FontIcon x:Name="LockIcon" Glyph="&#xE72E;" FontSize="12" Foreground="#80000000" />
                </Button>
                <!-- Click-Through button -->
                <Button x:Name="ClickThroughButton" Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0" Click="ClickThroughButton_Click">
                    <FontIcon Glyph="&#xE8B0;" FontSize="12" Foreground="#80000000" />
                </Button>
                <!-- Close/Delete button -->
                <Button x:Name="DeleteButton" Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0" Click="DeleteButton_Click">
                    <FontIcon Glyph="&#xE74D;" FontSize="12" Foreground="#80000000" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="12,8,12,12">
            <TextBlock x:Name="ContentTextBlock" Text="Sticker content goes here..." TextWrapping="Wrap" FontSize="14" Foreground="#333333" DoubleTapped="ContentTextBlock_DoubleTapped" />
        </ScrollViewer>

        <!-- Custom resizer handle in bottom right corner -->
        <Canvas Grid.Row="1" Width="12" Height="12" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" PointerPressed="ResizeHandle_PointerPressed" PointerMoved="ResizeHandle_PointerMoved" PointerReleased="ResizeHandle_PointerReleased" Background="Transparent">
            <Path Data="M 12 0 L 12 12 L 0 12 Z" Fill="#20000000" />
        </Canvas>
    </Grid>
</Window>
